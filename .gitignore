node_modules/
.DS_Store
*.log

# Build output
dist/

# Runtime state when the example-agent dir is used as a dev agent home
# (the framework repo's root is never used as one). `.env` and `auth.json`
# carry secrets; the others are per-channel runtime created by the bot.
.env
auth.json
example-agent/sessions/
example-agent/workspaces/
example-agent/active.json
